apiVersion: 1

providers:
  - name: 'plugin-nodes'
    orgId: 1
    folder: 'Plugin Nodes'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
